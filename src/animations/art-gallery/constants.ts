// Target number of mosaic cells. The actual grid dimensions are derived per
// painting from its aspect ratio (see use-painting-analysis), so this is only an
// approximate cell budget, not an exact count.
export const TARGET_CELLS = 10000;
